[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "social-embodied"
version = "0.1.0"
description = "Benchmark framework for social embodied reasoning in VirtualHome."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["social_embodied*"]

